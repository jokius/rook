# downloaded prebuilt libghostty (never committed — see scripts/setup.sh)
# no trailing slash so the symlink form (worktree setup) is ignored too, not just the dir
GhosttyKit.xcframework
rook/Resources/ghostty
rook/Resources/terminfo
*.tar.gz

# build artifacts
build/
.build/
rook.xcodeproj/
.swiftpm/

.DS_Store

# app state / runtime files (only appear if ROOK_STATE_DIR is pointed at the repo)
settings.json
windows.json
workspaces.json
windows/
*.sock

# track the path-scoped Claude rules (the global ~/.gitignore_global ignores .claude/)
!.claude/
.claude/*
!.claude/rules/
